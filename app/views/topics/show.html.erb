
<table class="table table-striped table-hover ">

 <thead>
    <tr>
	 <th width="10%">Author</th>
     <th width="80%">Post</th>
     <th width="10%">Views</th>
    </tr>
  </thead>
  
    <p>
      <strong>Topic:</strong>
      <%= @topic.subject %>
     </p>
	
	<%= link_to 'New Topic', new_forum_topic_path(@topic.forum_id), :class => "btn btn-primary" %> 
	<%= link_to 'Reply', new_topic_post_path(@topic.id), :class => "btn btn-primary" %> 
    
	 
    <tbody>
	
  <% for post in @topic.posts %>
  
    <tr>
	
	<td>
	<p>
     <strong>Last poster:</strong>
     <%= @topic.user_id %> </p>
	 </td>
	<td>
     <strong>Post:</strong>
     <%= markdown(post.text) %>
    </p>
	<p>
     <strong>Last poster:</strong>
     <%= @topic.user_id %>
    </p>

<p>
  <strong>Last post at:</strong>
  <%= post.created_at %>
</p>
    <%= link_to 'Reply', new_topic_post_path(@topic.id), :class => "btn btn-primary" %> 
    <%= link_to 'Edit', edit_topic_path(@topic.id), :class => "btn btn-primary" %> 
    <%= link_to 'Back', root_path, :class => "btn btn-primary" %>  
   </td>
   
<td><h4><span class="badge"><%= @topic.views_count  %></span></a> views</h4></td>
   
    </tr>
  <% end %>
   </tbody>
</table> 

